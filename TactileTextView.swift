import SwiftUI

// Compatibility shim for existing Xcode project references.
// The Loom engine now lives in ContentView + TactileTextEngine.
struct TactileTextView: View {
    var body: some View {
        EmptyView()
    }
}
